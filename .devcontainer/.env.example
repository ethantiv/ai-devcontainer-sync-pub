# DevContainer Environment Variables Example
# 
# This file serves as a template for .devcontainer/.env
# Copy this file to .devcontainer/.env for local development

# GitHub Personal Access Token (required)
# Get your token from: https://github.com/settings/tokens
# Required permissions: repo, read:user, workflow
# GH_TOKEN=your_github_token_here

# SSH Private Key (optional, for GitHub authentication via SSH)
# Encode your private key with: base64 -w 0 ~/.ssh/id_rsa
# SSH_PRIVATE_KEY=base64_encoded_ssh_private_key

# Authentication tokens are no longer managed by this script
# Configure Claude authentication manually if needed

# Context7 MCP Server API Key (optional)
# Get your key from: https://upstash.com/
# CONTEXT7_API_KEY=your_context7_api_key_here

# Reset configuration flags (optional)
# Set to "true" to clear all config on next container rebuild
# Useful for troubleshooting plugin issues (clears ~/.claude/plugins/cache)
# See: https://code.claude.com/docs/en/discover-plugins#troubleshooting
# RESET_CLAUDE_CONFIG=true
# RESET_GEMINI_CONFIG=true

# Note for GitHub Codespaces users:
# - GitHub tokens are provided through Codespaces secrets automatically
# - SSH keys and auth files can be added as Codespaces secrets
# - This empty .env file is still required for DevContainer runArgs
# 
# IMPORTANT: The actual .env file is in .gitignore to prevent token exposure